Diretório de headers do firmware.

- **`config.example.h`** — template de configuração (Wi-Fi, API, pinos, calibração). Copie para `config.h` e edite; `config.h` está no `.gitignore`.
- Demais headers do projeto são incluídos a partir de `src/` com `#include "..."`.
